apiVersion: v1
kind: Service
metadata:
  name: {{ include "todos-spa.fullname" . }}
spec:
  selector:
    app: {{ include "todos-spa.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
