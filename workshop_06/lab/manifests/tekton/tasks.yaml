# ── Task: buildah-build-push ────────────────────────────────────────────────
# Builduje kontajner pomocou buildah a pushuje ho do OpenShift interného registra.
# Používa rootless build s VFS storage driverom (bezpečné bez privilegovaného SCC).
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: buildah-build-push
  namespace: workshop-06-cicd
spec:
  params:
    - name: image
      type: string
      description: "Plná cesta k image vrátane tagu, napr. image-registry.openshift-image-registry.svc:5000/workshop-06-cicd/greeter:abc123"
    - name: containerfile
      type: string
      default: Containerfile
      description: "Názov Containerfile/Dockerfile"
    - name: context
      type: string
      default: "."
      description: "Build context relatívne k workspaces.source.path/src"
  workspaces:
    - name: source
  steps:
    - name: build-and-push
      image: quay.io/buildah/stable:latest
      workingDir: $(workspaces.source.path)/src
      securityContext:
        runAsUser: 0
      env:
        - name: STORAGE_DRIVER
          value: vfs
      script: |
        #!/bin/bash
        set -e
        echo "[buildah] Building image: $(params.image)"
        buildah --storage-driver=vfs bud \
          --no-cache \
          -f "$(params.containerfile)" \
          -t "$(params.image)" \
          "$(params.context)"

        echo "[buildah] Logging into OCP internal registry..."
        # Použi token aktuálneho SA pre prihlásenie do registra
        buildah --storage-driver=vfs login \
          --tls-verify=false \
          -u serviceaccount \
          -p "$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)" \
          image-registry.openshift-image-registry.svc:5000

        echo "[buildah] Pushing image: $(params.image)"
        buildah --storage-driver=vfs push \
          --tls-verify=false \
          "$(params.image)"

        echo "[buildah] Done: $(params.image)"

---
# ── Task: npm ───────────────────────────────────────────────────────────────
# Spúšťa npm príkaz (install / run lint / run test) v zdrojovom adresári.
apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: npm
  namespace: workshop-06-cicd
spec:
  params:
    - name: script
      type: string
      description: "npm príkaz bez prefixu 'npm', napr. install, run lint, run test"
    - name: workingDir
      type: string
      default: src
      description: "Podadresár v workspace kde sa nachádza package.json"
  workspaces:
    - name: source
  steps:
    - name: npm
      image: node:20-alpine
      workingDir: $(workspaces.source.path)/$(params.workingDir)
      script: |
        #!/bin/sh
        set -e
        echo "[npm] Spúšťam: npm $(params.script)"
        npm $(params.script)
        echo "[npm] Hotovo: npm $(params.script)"
