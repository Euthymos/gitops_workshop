apiVersion: v1
kind: Service
metadata:
  name: httpd-web
  namespace: workshop-03
spec:
  type: ClusterIP
  selector:
    app: httpd-web
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP