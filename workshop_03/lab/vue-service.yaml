apiVersion: v1
kind: Service
metadata:
  name: vue-web
  namespace: workshop-03
spec:
  type: NodePort
  selector:
    app: vue-web
  ports:
    - port: 8080        # port služby
      targetPort: 8080  # port v kontajneri
      protocol: TCP